# VulnTrack SCA — Static Edition

Personal vulnerability tracking console served via GitHub Pages.

- All data is stored in YOUR browser only (localStorage). Nothing is sent to a server.
- First sign-in sets a device PIN (privacy screen, client-side).
- Full edition with a real backend: see the private vulntrack-sca repo.
